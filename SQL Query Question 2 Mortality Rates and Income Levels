

This query selects the infant mortality indicator, country info and year along with calculating the average value of infant mortality rate for the 12 countries in this project. The average value is grouped by country info and year. 
SELECT indicator_name,
  country_code,
  country_name, 
  year,
  ROUND(AVG(value),2) AS average_mortality_value
 FROM `bigquery-public-data.world_bank_wdi.indicators_data` 
 WHERE indicator_name LIKE '%Mortality rate, infant%'
  AND country_code IN ('CAN','NGA', 'FRA', 'AFG', 'MLI', 'USA', 'COL', 'COG', 'DOM', 'GHA', 'IND', 'ZAF')
  AND year BETWEEN 1992 AND 2022
GROUP BY 
  country_name, country_code, year, indicator_name


This query selects the neonatal mortality indicator, country info and year along with calculating the average value of infant mortality rate for the 12 countries in this project. The average value is grouped by country info and year. 
SQL Query 2: Average Neontal Mortality Rate 1992 to 2022
SELECT indicator_name,
  country_code,
  country_name, 
  year,
  ROUND(AVG(value),2) AS average_mortality_value
 FROM `bigquery-public-data.world_bank_wdi.indicators_data` 
 WHERE indicator_name LIKE '%Mortality rate, neonatal%'
  AND country_code IN ('CAN','NGA', 'FRA', 'AFG', 'MLI', 'USA', 'COL', 'COG', 'DOM', 'GHA', 'IND', 'ZAF')
  AND year BETWEEN 1992 AND 2022
GROUP BY 
  country_name, country_code, year, indicator_name

This query selects the mortality for under 5 years old indicator, country info and year along with calculating the average value of infant mortality rate for the 12 countries in this project. The average value is grouped by country info and year. 
SQL Query 3: Average Mortality for less than 5 year olds Rate 1992 to 2022
SELECT indicator_name,
  country_code,
  country_name, 
  year,
  ROUND(AVG(value),2) AS average_mortality_value
 FROM `bigquery-public-data.world_bank_wdi.indicators_data` 
 WHERE indicator_name LIKE '%Mortality rate, under-5%'
  AND country_code IN ('CAN','NGA', 'FRA', 'AFG', 'MLI', 'USA', 'COL', 'COG', 'DOM', 'GHA', 'IND', 'ZAF')
  AND year BETWEEN 1992 AND 2022
GROUP BY 
  country_name, country_code, year, indicator_name


This query selects the adult mortality indicator, country info and year along with calculating the average value of infant mortality rate for the 12 countries in this project. The average value is grouped by country info and year. 
SQL Query 4: Average High Mortality Rates 1992 to 2022
SELECT indicator_name,
  country_code,
  country_name, 
  year,
  ROUND(AVG(value),2) AS average_mortality_value
 FROM `bigquery-public-data.world_bank_wdi.indicators_data` 
 WHERE indicator_name LIKE '%Mortality rate, adult%'
  AND country_code IN ('CAN','NGA', 'FRA', 'AFG', 'MLI', 'USA', 'COL', 'COG', 'DOM', 'GHA', 'IND', 'ZAF')
  AND year BETWEEN 1992 AND 2022
GROUP BY 
  country_name, country_code, year, indicator_name
