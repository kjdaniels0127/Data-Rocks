
Select current health expediture (% of GDP) for 12 select countries between 2005-2013.
Calculate the average current health expenditure (% of GDP) grouped by country info and year.
SELECT country_code, year, indicator_name, 
ROUND(AVG(value), 2) AS average_value,
FROM `bigquery-public-data.world_bank_wdi.indicators_data` 
WHERE country_code IN (
'USA','CAN','ZAF','NGA','FRA','AFG',
'COL','COG','GHA','IND','MLI','DOM'
)
AND indicator_name = 'Current health expenditure (% of GDP)' 
AND year BETWEEN 2005 and 2013
GROUP BY country_code, year, indicator_name 

Select current health expediture (% of GDP) for 12 select countries between 2014-2022.
Calculate the average current health expenditure (% of GDP) grouped by country info and year.
SELECT country_code, year, indicator_name, 
ROUND(AVG(value), 2) AS average_value,
FROM `bigquery-public-data.world_bank_wdi.indicators_data` 
WHERE country_code IN (
'USA','CAN','ZAF','NGA','FRA','AFG',
'COL','COG','GHA','IND','MLI','DOM'
)
AND indicator_name = 'Current health expenditure (% of GDP)' 
AND year BETWEEN 2014 and 2022
GROUP BY country_code, year, indicator_name

Select HIV Incidence rate abouve 50 for 12 select countries (excluded France and Canada) between 2005-2013.
SELECT indicator_name AS measuring_factor,
 ROUND(value, 2) AS measuring_value,
 country_code, 
 country_name,
 year,
 FROM `bigquery-public-data.world_bank_health_population.health_nutrition_population` 
WHERE indicator_name LIKE '%Incidence of HIV, ages 50+ (per 1,000 uninfected population ages 50+)%'
AND year BETWEEN 2005 and 2013
AND country_code IN (
'AFG','MLI','COG','GHA','NGA','DOM',
'IND','COL','ZAF','CAN','FRA','USA'
)

Select HIV Incidence rate abouve 50 for 12 select countries (excluded France and Canada) between 2014-2020.
SELECT indicator_name AS measuring_factor,
 ROUND(value, 2) AS measuring_value,
 country_code, 
 country_name,
 year,
 FROM `bigquery-public-data.world_bank_health_population.health_nutrition_population` 
WHERE indicator_name LIKE '%Incidence of HIV, ages 50+ (per 1,000 uninfected population ages 50+)%'
AND year BETWEEN 2014 and 2020
AND country_code IN (
'AFG','MLI','COG','GHA','NGA','DOM',
'IND','COL','ZAF','CAN','FRA','USA'
) 

Select HIV Incidence rate between ages 15-49 for 12 select countries (excluded France and Canada) between 2014-2020
Calculate the average HIV Incidence rate between ages 15-49 grouped by country info and years.
SELECT indicator_name AS measuring_factor,
 ROUND(AVG(value), 2) AS average_value,
 country_code, 
 country_name,
 year,
 FROM `bigquery-public-data.world_bank_health_population.health_nutrition_population` 
WHERE indicator_name LIKE '%Incidence of HIV, ages 15-49 (per 1,000 uninfected population ages 15-49)%'
AND year BETWEEN 2014 and 2020
AND country_code IN (
'AFG','MLI','COG','GHA','NGA','DOM',
'IND','COL','ZAF','CAN','FRA','USA'
)
GROUP BY indicator_name, year, country_code, country_name

Select HIV Incidence rate between ages 15-49 for 12 select countries (excluded France and Canada) between 2005-2013.
Calculate the average HIV Incidence rate between ages 15-49 grouped by country info and years.
SELECT indicator_name AS measuring_factor,
 ROUND(AVG(value), 2) AS average_value,
 country_code, 
 country_name,
 year,
 FROM `bigquery-public-data.world_bank_health_population.health_nutrition_population` 
WHERE indicator_name LIKE '%Incidence of HIV, ages 15-49 (per 1,000 uninfected population ages 15-49)%'
AND year BETWEEN 2005 and 2013
AND country_code IN (
'AFG','MLI','COG','GHA','NGA','DOM',
'IND','COL','ZAF','CAN','FRA','USA'
)
GROUP BY indicator_name, year, country_code, country_name

Select HIV Incidence rate for all people for 12 select countries (excluded France and Canada) between 2005-2013.
SELECT indicator_name AS measuring_factor,
ROUND(value,2) AS measuring_value,
country_code,
country_name,
year,
 FROM `bigquery-public-data.world_bank_health_population.health_nutrition_population` 
WHERE indicator_name LIKE '%Incidence of HIV, all (per 1,000 uninfected population)%'
AND year BETWEEN 2005 and 2013
AND country_code IN (
'USA','FRA','CAN','ZAF','COL','IND',
'DOM','NGA', 'GHA', 'COG', 'MLI', 'AFG'
)


Select HIV Incidence rate for all people for 12 select countries (excluded France and Canada) between 2014-2020.
SELECT indicator_name AS measuring_factor,
ROUND(value,2) AS measuring_value,
country_code,
country_name,
year,
 FROM `bigquery-public-data.world_bank_health_population.health_nutrition_population` 
WHERE indicator_name LIKE '%Incidence of HIV, all (per 1,000 uninfected population)%'
AND year BETWEEN 2014 and 2020
AND country_code IN (
'USA','FRA','CAN','ZAF','COL','IND',
'DOM','NGA', 'GHA', 'COG', 'MLI', 'AFG'
)
